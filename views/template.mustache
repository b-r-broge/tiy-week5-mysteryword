<!DOCTYPE html>
<html>

<head>
  <meta charset="utf-8">
  <title>Mystery Words</title>
</head>

<body>
  <h1>Mystery Words</h1>
  <h3>Guesses Remaining: {{remGuess.guessesRemaining}}</h3>

  <div class="game-area">
    {{#wordArr}}
      {{#seen}}
        <span>{{letter}}</span>
      {{/seen}}
      {{^seen}}
      <span>_</span>
      {{/seen}}
    {{/wordArr}}
  </div>
  <div class="guess-area">
    <form class="" action="/guess" method="post">
      <input type="text" name="nextLetter" value="" autofocus="true" required pattern="(?![{{#letGuess}}{{.}}{{/letGuess}}{{#lowGuess}}{{.}}{{/lowGuess}}])[A-z]" title="Please insert one alpha character, excluding any already guessed">
      <button type="submit" name="button"></button>
    </form>
    <p>letters guessed:
      {{#letGuess}}
        {{.}}
      {{/letGuess}}
    </p>
  </div>
  <div class="nav">
    <a href="/restart">Start again!</a>
    <a href="/champion">Check the Leader Board</a>
  </div>
</body>

</html>
