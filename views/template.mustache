<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Mystery Words</title>
  </head>
  <body>
    <h1>Mystery Words</h1>
    <h3>Guesses Remaining: {{remGuess.guessesRemaining}}</h3>

    <div class="game-area">
      {{#wordArr}}
        {{#seen}}
          <span>{{letter}}</span>
        {{/seen}}
        {{^seen}}
          <span>_</span>
        {{/seen}}
      {{/wordArr}}
    </div>
    <div class="guess-area">
      <form class="" action="/guess" method="post">
        <input type="text" name="nextLetter" value="">
        <button type="submit" name="button"></button>
      </form>
      <p>letters guessed:
        {{#letGuess}}
        <span> {{.}}
        {{/letGuess}}
      </p>
    </div>
  </body>
</html>

<!-- {info: [req.session.word,
        req.session.wordArr, {letter: a, seen: false}
        req.session.remGuess,
        req.session.letGuessed,
        req.session.letter,
        req.session.visit]} -->
